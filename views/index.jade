extends layout

block content
    - if (user)
        a(href="/user/" + user.id)
            h5 Welcome to BUAA #{title},#{user.nickname}
    .col-lg-6
        h4.text-center 正在进行的比赛......

    .col-lg-6
        h4.text-center(href="/group") 活跃的小组......