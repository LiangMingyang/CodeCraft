extends ../layout
block content
    - for (var i = 0 ; i < submissions.length ; ++i)
        - var submission = submissions[i];
        p #{submission.id} #{submission.creator.nickname} #{submission.result}
